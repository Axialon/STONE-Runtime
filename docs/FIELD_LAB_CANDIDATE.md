# Field Lab acceptance record

The earlier candidate's blocking presentation regression is resolved in the current work: the drone comparison label is host-specific, each Stone has a distinct colour and silhouette, and camera Focus plus host-specific accessibility labels are implemented. The obsolete hidden planned-drone claim is removed. The failing checks were retained and now pass; no physics thresholds or host limits were relaxed.

The live workbench combines actual drone/anchored-arm sessions and local Digital Stones. It also includes core v0.2 manifest export and AUDIT file inspection. See README.md for running it, CORE_MANIFEST.md for metadata semantics and EVIDENCE_INSPECTION.md for supported evidence. The original rover remains separately linked, not replaced or merged into the same scene.

Before accepting the earlier draft PR, run every normal, fault and inspection browser check and the original rover regression from the exact accepted source. Current verification evidence is in evidence/FIELD_LAB_V0_1.md. This document records the resolution of the earlier candidate, not a claim that past failed runs were successes. The public Git commit and PR merge state remain the authority for accepted integration.

No trained weights, provider call, public app deployment, physical-machine operation or ClawSpan integration is implied by a working local interface. Replay reexecution is not publisher authentication. Current review is self-review and platform/accessibility limits remain in the evidence record.
