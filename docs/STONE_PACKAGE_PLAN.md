# Portable Stone file implementation plan

Outcome: carry the reviewed FLOW model and operating requirements as bounded data, inspect it locally, and explicitly activate it on the compatible preinstalled runtime. This extends the accepted live-model work; it does not install arbitrary code, grant permissions or certify publishers.

- [x] Versioned256KiB package parser and reviewed-model admission; integrity-invalid and consistent-but-unknown data are distinct.
- [x] Reference export and supplied-parameter loading; inference, comparisons and replay retain that source without hidden server-weight fallback.
- [x] Native-file draft inspection, explicit activation/reset, source receipts, stale/disposed read protection and failed-operation recovery.
- [x] Digital AUDIT package-only inspection without loading model inference; actual policy-record verification remains separate.
- [x] Ready-to-import example generated only from the fixed model and reference manifest, with exact regeneration coverage.
- [x] Independent full acceptance:637 distinct Node tests and159 sandboxed browser cases, four unchanged offline lockfile installs and reproducible bundle build.
- [x] Distinct source reviewer and scoped correction/re-review for native cancel handling. Browser tests exercise cancel events on native populated inputs, not graphical OS-dialog automation.
- [ ] Final publication, private pin/current record and immutable preview activation are integration steps. The PR and coordinator execution board record their outcome; this plan does not assert a future merge.

No model/protocol change, retraining, dependency addition, spending, provider activation, physical actuation, public app deployment or ClawSpan change. Only the known FLOW artifact and reviewed adapter/operating metadata can activate. Source/model remain UNLICENSED; upstream notices remain intact.

Usage: STONE_FILES.md. Final product evidence: ../evidence/PORTABLE_STONES_V0_1.md. The earlier STONE_FILES_IMPLEMENTATION.md is the native writer's pre-acceptance record, not the final status. The supervising coordinator owns integration and source identities; no protected Git or browser sandbox was loosened to make native execution appear complete.
