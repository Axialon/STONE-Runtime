{
  "name": "@stone/rover3d-experiment",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:contract": "node --test tests/contract.test.mjs tests/preflight.test.mjs",
    "test:engine": "node --test tests/engine.test.mjs",
    "preflight": "node preflight.mjs",
    "verify": "npm run test:contract && npm run preflight && npm run test:engine"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "0.20.0"
  }
}
