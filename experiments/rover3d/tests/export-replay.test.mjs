import test from 'node:test';import assert from 'node:assert/strict';import {loadRapier} from '../engine.mjs';import {createSession,replayRecording} from '../session.mjs';
test('export can be re-executed after the original worker world is disposed',async()=>{const R=await loadRapier(),s=createSession(R,'flat-lane','rover.flow');s.advance(60);s.select('rover.dart');s.advance(60);const last=s.read(),text=JSON.stringify(s.export());s.dispose();const r=replayRecording(R,text);assert.deepEqual(r.frames.at(-1).frame,last.frame);assert.equal(r.frames.length,121);assert.equal(r.frames[80].appliedStoneId,'rover.dart');});
test('bad recording is rejected before creating a world',()=>{for(const text of ['{}','null','not json','x'.repeat(2100000)])assert.throws(()=>replayRecording(null,text));});
