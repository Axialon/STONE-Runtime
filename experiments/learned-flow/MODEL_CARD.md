# FLOW / Learned 0.1.0

Status: evaluated experimental policy; now also available through explicit fixed-lane browser opt-in. The original model and offline protocol/results below remain unchanged. See ../../docs/LIVE_MODEL_STONES.md from repository paths for the separately tested live integration.

Artifact: model/model.json; SHA-256 d9a1ef32fa555b5f9d8fdd326a90b59f8c03228b3854564d99bed708d0a210c2.
Size:29,454 bytes of JSON,203 nodes,102 scalar leaves,maximum depth8. Model parameters are fitted from4021 training examples generated by the unchanged FLOW rule and actual Rapier rover simulations. A checksum identifies bytes, not publisher authenticity.

Training: ml-cart2.1.1 DecisionTreeRegression; gainFunction regression, splitFunction mean, maxDepth8, minNumSamples8. The latter is the library's node stopping option, not a promise that every final leaf contains eight samples. No neural training or custom-silicon implementation is claimed.

Inputs: current horizontal goal distance, heading error, signed forward speed, forward vertical component. Output: signed throttle/brake demand. The harness retains deterministic steering, guards, command bounds, engine authority and termination. Explicit out-of-domain handling is a brake guard, not a teacher-policy fallback.

Data:48 training,16 validation and24 test teacher episodes from disjoint seed partitions on the same two fixed lanes. Both course variants of a seed share a partition. Small deterministic warm-up command tapes change the starting conditions without changing physics. Data sampled every sixth tick before the next teacher action, after warm-up; guarded/out-of-domain rows are excluded. Separate seed0 references are not held-out evidence. No test tuning occurred.

Results:16/16 validation and24/24 test model-plus-harness completions, matching the baseline completion counts;2/2 separate reference completions. Every successful learned episode involved actual model calls. Model calls on test episodes:11,540; out-of-domain fallback steps:0. Zero chassis-contact starts on test episodes. These are simulated checks, not proof of real-machine safety or model superiority.

Test signed-demand mean absolute error:0.0063703 on1975 teacher-state rows. Maximum signed-demand absolute error:0.3175961. Small mean error does not exclude larger individual errors. Validation errors are separate. Complete per-episode outcomes and stopping-distance/time trade-offs are in model/evaluation-summary.json.

Reproducibility: exact model/partition-row bytes,88 teacher-episode records and42 paired learned outcomes/action tapes reproduced on a second full run.7326 teacher rows matched saved/reloaded predictions and decoded actions. Exact replay is established on Linux x64, Node22.20.0, V8 12.4.254.21-node.33, Rapier0.20.0, not guaranteed across other platforms. The runner records environment, source hashes, lock integrities, installed-file inventories and coverage of actually loaded CommonJS files. Native ESM Rapier files are inventoried separately.

Boundaries: no publisher signature, general-navigation claim, quantisation, sensor/noise robustness, arbitrary third-party loading, physical-machine readiness, live cloud inference or integrated browser model selection. Resource requests in the manifest are provisional metadata, not measured or enforced quotas. This is an imitation experiment, not an improvement claim. Further selection must use a newly declared experiment and untouched test seeds.

Live integration note: browser inference and policy-aware recording/inspection have separate execution tests. No new learning or test-set tuning occurred. Custom route use remains excluded from model mode. Historical limitations concerning publisher trust, sensor robustness, unseen environments and hardware continue to apply.
